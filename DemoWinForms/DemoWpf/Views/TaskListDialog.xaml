<Window x:Class="DemoWpf.Views.TaskListDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
     xmlns:vm="clr-namespace:DemoWpf.ViewModels"
        d:DataContext="{d:DesignInstance Type=vm:TaskListDialogViewModel}"
        mc:Ignorable="d"
        Title="{Binding DialogTitle}"
        Height="450" Width="500"
        WindowStartupLocation="CenterOwner"
   ResizeMode="NoResize"
  WindowStyle="SingleBorderWindow">
    
    <Window.Resources>
        <Style x:Key="ColorButton" TargetType="Button">
 <Setter Property="Width" Value="40"/>
    <Setter Property="Height" Value="40"/>
   <Setter Property="Margin" Value="5"/>
 <Setter Property="BorderThickness" Value="2"/>
   <Setter Property="BorderBrush" Value="Gray"/>
          <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
<RowDefinition Height="Auto"/>
 <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="Nombre de la lista *" FontWeight="SemiBold" Margin="0,0,0,5"/>
<TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
         MaxLength="100"
         Padding="8"
            FontSize="14"/>
 <TextBlock Text="{Binding Name.Length, StringFormat='{}{0}/100 caracteres'}"
         FontSize="11"
     Foreground="Gray"
             Margin="0,2,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,0,0,15">
     <TextBlock Text="Descripcion (opcional)" FontWeight="SemiBold" Margin="0,0,0,5"/>
     <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
    MaxLength="500"
         Height="80"
            Padding="8"
           TextWrapping="Wrap"
            AcceptsReturn="True"
 VerticalScrollBarVisibility="Auto"/>
         <TextBlock Text="{Binding Description.Length, StringFormat='{}{0}/500 caracteres'}"
       FontSize="11"
        Foreground="Gray"
          Margin="0,2,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,0,0,15">
<TextBlock Text="Color identificativo" FontWeight="SemiBold" Margin="0,0,0,10"/>
      <WrapPanel>
          <Button Style="{StaticResource ColorButton}"
          Background="#4A90E2"
             Command="{Binding SelectColorCommand}"
               CommandParameter="#4A90E2"
        ToolTip="Azul"/>
      <Button Style="{StaticResource ColorButton}"
    Background="#5BC0DE"
          Command="{Binding SelectColorCommand}"
   CommandParameter="#5BC0DE"
      ToolTip="Azul claro"/>
       <Button Style="{StaticResource ColorButton}"
        Background="#5CB85C"
      Command="{Binding SelectColorCommand}"
             CommandParameter="#5CB85C"
       ToolTip="Verde"/>
    <Button Style="{StaticResource ColorButton}"
    Background="#F0AD4E"
        Command="{Binding SelectColorCommand}"
          CommandParameter="#F0AD4E"
         ToolTip="Naranja"/>
       <Button Style="{StaticResource ColorButton}"
            Background="#D9534F"
    Command="{Binding SelectColorCommand}"
  CommandParameter="#D9534F"
 ToolTip="Rojo"/>
 <Button Style="{StaticResource ColorButton}"
    Background="#9B59B6"
 Command="{Binding SelectColorCommand}"
  CommandParameter="#9B59B6"
 ToolTip="Purpura"/>
    <Button Style="{StaticResource ColorButton}"
         Background="#34495E"
               Command="{Binding SelectColorCommand}"
            CommandParameter="#34495E"
           ToolTip="Gris oscuro"/>
         </WrapPanel>
            
     <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
        <TextBlock Text="Color seleccionado:" VerticalAlignment="Center" Margin="0,0,10,0"/>
    <Border Width="60" Height="30" 
  Background="{Binding ColorCode}"
   BorderBrush="Gray"
        BorderThickness="1"
         CornerRadius="3"/>
              <TextBlock Text="{Binding ColorCode}" 
 VerticalAlignment="Center"
 Margin="10,0,0,0"
    FontFamily="Consolas"/>
   </StackPanel>
        </StackPanel>

     <TextBlock Grid.Row="3"
          Text="{Binding ValidationMessage}"
     Foreground="Red"
    FontWeight="SemiBold"
              Margin="0,0,0,10"
       TextWrapping="Wrap"/>

     <Grid Grid.Row="4"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
      <Button Content="Cancelar"
      Command="{Binding CancelCommand}"
  CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
           Width="100"
  Height="35"
        Margin="0,0,10,0"
       Background="#95A5A6"
        Foreground="White"
      BorderThickness="0"
           Cursor="Hand"/>
     <Button Content="Guardar"
        Command="{Binding SaveCommand}"
            CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
        Width="100"
      Height="35"
        Background="#5CB85C"
     Foreground="White"
     BorderThickness="0"
        Cursor="Hand"/>
        </StackPanel>
</Grid>
</Window>
